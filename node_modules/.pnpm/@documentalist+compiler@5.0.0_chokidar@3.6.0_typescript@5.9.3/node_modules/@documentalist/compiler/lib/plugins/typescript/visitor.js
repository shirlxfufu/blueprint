"use strict";
/**
 * Copyright 2017 Palantir Technologies, Inc. All rights reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.Visitor = void 0;
const client_1 = require("@documentalist/client");
const path_1 = require("path");
const typedoc_1 = require("typedoc");
const typestring_1 = require("./typestring");
class Visitor {
    constructor(compiler, options) {
        this.compiler = compiler;
        this.options = options;
        this.visitClass = (def) => (Object.assign(Object.assign({}, this.visitInterface(def)), { accessors: this.visitChildren(def.getChildrenByKind(typedoc_1.ReflectionKind.Accessor), this.visitAccessor), constructorType: this.visitChildren(def.getChildrenByKind(typedoc_1.ReflectionKind.Constructor), this.visitConstructor)[0], kind: client_1.Kind.Class }));
        this.visitInterface = (def) => {
            var _a, _b;
            return (Object.assign(Object.assign({}, this.makeDocEntry(def, client_1.Kind.Interface)), { extends: (_a = def.extendedTypes) === null || _a === void 0 ? void 0 : _a.map(typestring_1.resolveTypeString), implements: (_b = def.implementedTypes) === null || _b === void 0 ? void 0 : _b.map(typestring_1.resolveTypeString), indexSignature: def.indexSignature && this.visTsignature(def.indexSignature), methods: this.visitChildren(def.getChildrenByKind(typedoc_1.ReflectionKind.Method), this.visitMethod, sortStaticFirst), properties: this.visitChildren(def.getChildrenByKind(typedoc_1.ReflectionKind.Property), this.visitProperty, sortStaticFirst) }));
        };
        this.visitConstructor = (def) => (Object.assign(Object.assign({}, this.visitMethod(def)), { kind: client_1.Kind.Constructor }));
        this.visitEnum = (def) => (Object.assign(Object.assign({}, this.makeDocEntry(def, client_1.Kind.Enum)), { members: this.visitChildren(def.getChildrenByKind(typedoc_1.ReflectionKind.EnumMember), m => (Object.assign(Object.assign({}, this.makeDocEntry(m, client_1.Kind.EnumMember)), { defaultValue: getDefaultValue(m) }))) }));
        this.visitProperty = (def) => (Object.assign(Object.assign({}, this.makeDocEntry(def, client_1.Kind.Property)), { defaultValue: getDefaultValue(def), inheritedFrom: def.inheritedFrom && (0, typestring_1.resolveTypeString)(def.inheritedFrom), type: (0, typestring_1.resolveTypeString)(def.type) }));
        this.visitMethod = (def) => (Object.assign(Object.assign({}, this.makeDocEntry(def, client_1.Kind.Method)), { inheritedFrom: def.inheritedFrom && (0, typestring_1.resolveTypeString)(def.inheritedFrom), signatures: def.signatures !== undefined ? def.signatures.map(sig => this.visTsignature(sig)) : [] }));
        this.visTsignature = (sig) => (Object.assign(Object.assign({}, this.makeDocEntry(sig, client_1.Kind.Signature)), { flags: undefined, parameters: (sig.parameters || []).map(param => this.visitParameter(param)), returnType: (0, typestring_1.resolveTypeString)(sig.type), type: (0, typestring_1.resolveSignature)(sig) }));
        this.visitParameter = (param) => (Object.assign(Object.assign({}, this.makeDocEntry(param, client_1.Kind.Parameter)), { defaultValue: getDefaultValue(param), sourceUrl: undefined, type: (0, typestring_1.resolveTypeString)(param.type) }));
        this.visitAccessor = (param) => {
            var _a, _b;
            let type;
            let getDocumentation;
            let setDocumentation;
            if (param.getSignature) {
                type = (0, typestring_1.resolveTypeString)(param.getSignature.type);
            }
            else if (((_a = param.setSignature) === null || _a === void 0 ? void 0 : _a.parameters) && ((_b = param.setSignature) === null || _b === void 0 ? void 0 : _b.parameters[0]) !== undefined) {
                type = (0, typestring_1.resolveTypeString)(param.setSignature.parameters[0].type);
            }
            else {
                throw Error("Accessor did neither define get nor set signature.");
            }
            if (param.getSignature) {
                getDocumentation = this.renderComment(param.getSignature.comment);
            }
            if (param.setSignature) {
                setDocumentation = this.renderComment(param.setSignature.comment);
            }
            return Object.assign(Object.assign({}, this.makeDocEntry(param, client_1.Kind.Accessor)), { getDocumentation,
                setDocumentation,
                type });
        };
    }
    visitProject(project) {
        const { excludePaths = [] } = this.options;
        // get top-level members of typedoc project
        return [
            ...this.visitChildren(project.getReflectionsByKind(typedoc_1.ReflectionKind.Class), this.visitClass),
            ...this.visitChildren(project.getReflectionsByKind(typedoc_1.ReflectionKind.Enum), this.visitEnum),
            ...this.visitChildren(project.getReflectionsByKind(typedoc_1.ReflectionKind.Function), this.visitMethod),
            ...this.visitChildren(project.getReflectionsByKind(typedoc_1.ReflectionKind.Interface), this.visitInterface),
            ...this.visitChildren(project.getReflectionsByKind(typedoc_1.ReflectionKind.TypeAlias), def => (Object.assign(Object.assign({}, this.makeDocEntry(def, client_1.Kind.TypeAlias)), { type: (0, typestring_1.resolveTypeString)(def.type) }))),
        ].filter(
        // remove members excluded by path option
        ref => isNotExcluded(excludePaths, ref.fileName));
    }
    makeDocEntry(ref, kind) {
        var _a;
        let comment = ref.comment;
        if (comment === undefined && ref.isDeclaration()) {
            // special case for interface properties which have function signatures - we need to go one level deeper
            // to access the comment
            (_a = ref.type) === null || _a === void 0 ? void 0 : _a.visit({
                reflection: reflectionType => {
                    if (reflectionType.declaration.signatures !== undefined) {
                        comment = reflectionType.declaration.signatures[0].comment;
                    }
                },
            });
        }
        return {
            documentation: this.renderComment(comment),
            fileName: getSourceFileName(ref),
            flags: getFlags(ref),
            kind,
            name: ref.name,
            sourceUrl: getSourceUrl(ref),
        };
    }
    /** VisTs each child that passes the filter condition (based on options). */
    visitChildren(children, visitor, comparator) {
        const { excludeNames = [], excludePaths = [] } = this.options;
        return children
            .map(visitor)
            .filter(doc => isNotExcluded(excludeNames, doc.name) && isNotExcluded(excludePaths, doc.fileName))
            .sort(comparator);
    }
    /**
     * Converts a typedoc comment object to a rendered `Block`.
     */
    renderComment(comment) {
        if (comment === undefined) {
            return;
        }
        let documentation = "";
        documentation += comment.summary.map(part => part.text).join("\n");
        const blockTags = comment.blockTags.filter(tag => tag.tag !== "@default" && tag.tag !== "@deprecated");
        if (blockTags.length > 0) {
            documentation += "\n\n";
            documentation += blockTags.map(tag => `${tag.tag} ${tag.content}`).join("\n");
        }
        return this.compiler.renderBlock(documentation);
    }
}
exports.Visitor = Visitor;
function getCommentTagValue(comment, tagName) {
    const maybeTag = comment === null || comment === void 0 ? void 0 : comment.getTag(`@${tagName}`);
    return maybeTag === null || maybeTag === void 0 ? void 0 : maybeTag.content.map(part => part.text.trim()).join("\n");
}
function getDefaultValue(ref) {
    var _a, _b, _c, _d;
    // N.B. TypeDoc no longer sets defaultValue for enum members as of v0.23, see https://typedoc.org/guides/changelog/#v0.23.0-(2022-06-26)
    // Also, we typically expect enum member values to only have literal types, so we can just use the type value.
    if (ref.kind === typedoc_1.ReflectionKind.EnumMember && ((_a = ref.type) === null || _a === void 0 ? void 0 : _a.type) === "literal") {
        return (_c = (_b = ref.type) === null || _b === void 0 ? void 0 : _b.value) === null || _c === void 0 ? void 0 : _c.toString();
    }
    return (_d = ref.defaultValue) !== null && _d !== void 0 ? _d : getCommentTagValue(ref.comment, "default");
}
function getSourceFileName(reflection) {
    if (reflection.isDeclaration() || isSignatureReflection(reflection)) {
        if (reflection.sources !== undefined) {
            const { fullFileName } = reflection.sources[0];
            // fullFileName relative to cwd, so it can be saved in a snapshot (machine-independent)
            return fullFileName && (0, path_1.relative)(process.cwd(), fullFileName);
        }
    }
    return undefined;
}
function isSignatureReflection(reflection) {
    return reflection.variant === "signature";
}
function getSourceUrl(reflection) {
    var _a;
    if (reflection.isDeclaration() || isSignatureReflection(reflection)) {
        if (reflection.sources !== undefined) {
            return (_a = reflection.sources[0]) === null || _a === void 0 ? void 0 : _a.url;
        }
    }
    return undefined;
}
function getFlags(ref) {
    if (ref === undefined || ref.flags === undefined) {
        return undefined;
    }
    const isDeprecated = getIsDeprecated(ref);
    const { isExternal, isOptional, isPrivate, isProtected, isPublic, isRest, isStatic } = ref.flags;
    return {
        isDeprecated,
        isExternal,
        isOptional,
        isPrivate,
        isProtected,
        isPublic,
        isRest,
        isStatic,
    };
}
function getIsDeprecated(ref) {
    var _a;
    const deprecatedTagValue = getCommentTagValue(ref.comment, "deprecated");
    const deprecatedModifier = (_a = ref.comment) === null || _a === void 0 ? void 0 : _a.hasModifier("@deprecated");
    return deprecatedModifier || deprecatedTagValue !== undefined;
}
/** Returns true if value does not match all patterns. */
function isNotExcluded(patterns, value) {
    return value === undefined || patterns.every(p => value.match(p) == null);
}
/** Sorts static members (`flags.isStatic`) before non-static members. */
function sortStaticFirst({ flags: aFlags = {} }, { flags: bFlags = {} }) {
    if (aFlags.isStatic && bFlags.isStatic) {
        return 0;
    }
    else if (aFlags.isStatic) {
        return -1;
    }
    else if (bFlags.isStatic) {
        return 1;
    }
    return 0;
}
