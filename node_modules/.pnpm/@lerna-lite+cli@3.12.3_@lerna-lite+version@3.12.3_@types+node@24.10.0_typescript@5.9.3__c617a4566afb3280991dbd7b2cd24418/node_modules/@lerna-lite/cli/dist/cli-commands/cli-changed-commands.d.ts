import { ChangedCommandOption, ListCommandOption } from '@lerna-lite/core';
declare const _default: {
    command: string;
    aliases: string[];
    describe: string;
    builder: (yargs: any) => void;
    handler: (argv: ChangedCommandOption & ListCommandOption) => Promise<void>;
};
export default _default;
