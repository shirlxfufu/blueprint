import { Command, CommandType, InitCommandOption } from '@lerna-lite/core';
export declare function factory(argv: InitCommandOption): InitCommand;
export declare class InitCommand extends Command<InitCommandOption> {
    name: CommandType;
    exact?: boolean;
    lernaVersion: string;
    constructor(argv: InitCommandOption);
    get requiresGit(): boolean;
    runValidations(): void;
    runPreparations(): void;
    initialize(): Promise<any> | undefined;
    execute(): Promise<void>;
    ensurePackageJSON(): Promise<import("@lerna-lite/core").RawManifest>;
    ensureLernaConfig(): Promise<string>;
    ensurePackagesDir(): Promise<void[]>;
}
